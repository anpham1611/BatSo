<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.apmv.batso">

    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.apmv.batso.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        tools:replace="android:icon"
        android:theme="@style/PrimaryActivity">

        <!-- Custom Meta Data -->

        <meta-data
            android:name="API_SCHEME"
            android:value="https" />
        <meta-data
            android:name="API_DOMAIN"
            android:value="" />

        <activity
            android:name="com.apmv.batso.ui.activity.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/PrimaryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.apmv.batso.ui.activity.ServerWaitActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/PrimaryActivity" />
        <activity
            android:name="com.apmv.batso.ui.activity.ServerActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/PrimaryActivity" />
        <activity
            android:name=".ui.activity.ClientWaitActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/PrimaryActivity" />
        <activity
            android:name="com.apmv.batso.ui.activity.ClientActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/PrimaryActivity" />
        <activity
            android:name="com.apmv.batso.ui.activity.ReadMeActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/SecondaryActivity" />

    </application>

</manifest>
